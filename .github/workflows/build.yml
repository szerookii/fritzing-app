name: Build Fritzing App and Upload Artifacts

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Qt (Windows)
        if: matrix.os == 'windows-latest'
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.5.3'
          host: 'windows'
          arch: 'win64_msvc2019_64'

      - name: Set up Qt (macOS)
        if: matrix.os == 'macos-latest'
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.5.3'
          host: 'mac'
          arch: 'clang_64'

      - name: Install dependencies (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          choco install -y git cmake

      - name: Install dependencies (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          brew update
          brew install cmake

      - name: Build libgit2 (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          git clone --branch v1.7.1 --depth 1 https://github.com/libgit2/libgit2.git
          cd libgit2
          mkdir build
          cd build
          cmake .. -DBUILD_SHARED_LIBS=OFF -DCMAKE_BUILD_TYPE=Release -DBUILD_CLAR=OFF -DCMAKE_INSTALL_PREFIX=../../libgit2-1.7.1
          cmake --build . --config Release --target install
        shell: cmd

      - name: Build libgit2 (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          git clone --branch v1.7.1 --depth 1 https://github.com/libgit2/libgit2.git
          cd libgit2
          mkdir build
          cd build
          cmake .. -DBUILD_SHARED_LIBS=OFF -DCMAKE_BUILD_TYPE=Release -DBUILD_CLAR=OFF -DCMAKE_INSTALL_PREFIX=../../libgit2-1.7.1
          cmake --build . --config Release --target install

      # Compile Fritzing
      - name: Build (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          qmake
          cmake --build . --config Release

      - name: Build (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          qmake
          cmake --build . --config Release

      - name: Find build artifact (Windows)
        if: matrix.os == 'windows-latest'
        id: artifact-win
        run: |
          echo "artifact_path=$(Get-ChildItem -Path . -Filter fritzing.exe -Recurse | Select-Object -First 1 -ExpandProperty FullName)" >> $env:GITHUB_ENV

      - name: Find build artifact (macOS)
        if: matrix.os == 'macos-latest'
        id: artifact-mac
        run: |
          echo "artifact_path=$(find . -type d -name 'Fritzing.app' | head -n 1)" >> $GITHUB_ENV

      - name: Upload artifact (Windows)
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: fritzing-windows
          path: ${{ env.artifact_path }}

      - name: Upload artifact (macOS)
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: fritzing-macos
          path: ${{ env.artifact_path }}
